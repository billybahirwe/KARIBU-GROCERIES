doctype html
head
  meta(charset='UTF-8')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  title Sign Out
  link(rel='stylesheet' href='/css/signout.css')
.signout-container
  h2 Are you sure you want to sign out?
  button(onclick='confirmSignOut()') Sign Out
script(src='/js/signout.js')
